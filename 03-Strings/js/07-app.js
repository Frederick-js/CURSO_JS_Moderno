const producto = "Monitor 20 Pulgadas";
console.log(producto.toUpperCase()); // .toUpperCase convierte todo a mayuscula
console.log(producto.toLocaleLowerCase()); // .toUpperCase convierte todo a minuscula

const email = "correo@corre.com";

console.log(email.toLocaleLowerCase());

const precio = 300;
const precio2= "300";
console.log(precio.toString()); // convierte un numero a strin