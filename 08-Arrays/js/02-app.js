const numeros =  [10,20,30,40,50,[1,2,3]];

console.log(numeros);
console.table(numeros);


// Acceder al arreglo
console.table(numeros[2]); // mostraria 30 porque accedemos al indice 2
console.table(numeros[0]);
console.table(numeros[5][1]);
